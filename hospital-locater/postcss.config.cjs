module.exports = {
    plugins: {
        // The PostCSS plugin for Tailwind has moved to a separate package.
        // Use '@tailwindcss/postcss' and make sure that package is installed.
        '@tailwindcss/postcss': {},
        autoprefixer: {},
    },
}
